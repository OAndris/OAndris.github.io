$('document').ready(() => {
    $('header#injectedHeader').load('../html_components/header.html');
    $('aside#social-side').load('../html_components/social.html');
    $('footer#injectedFooter').load('../html_components/footer.html');
});
